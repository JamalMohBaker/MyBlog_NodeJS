<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MyBlog - Edit Post</title>
    <link href="/assets/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">MyBlog</a>
            <div class="navbar-nav">
                <a class="nav-link" href="admin-posts.html">All Posts</a>
                <a class="nav-link" href="/admin/posts/addPost">Add Post</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-12">
                <h2 class="mb-4">Edit Post</h2>
                <div class="card">
                    <div class="card-body">
                        <form id="editPost" data-post-id="<%= post._id %>">
                            <input type="hidden" name="author" id="author" value="<%= auth._id %>">
                            <div class="mb-3">
                                <label for="title" class="form-label">Title</label>
                                <input type="text" class="form-control" name="title" id="title" value="<%= post.title %>" required>
                            </div>
                            <div class="mb-3">
                                <label for="postText" class="form-label">Content</label>
                                <textarea class="form-control" name="postText" id="postText" rows="10" required><%= post.postText %>
                                </textarea>
                            </div>
                            
                            <div id="message"></div>
                           
                            <button type="submit" class="btn btn-primary">Update Post</button>
                            <a href="/admin/posts" class="btn btn-outline-secondary">Cancel</a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="/assets/js/ajax-post.js"></script>
</body>

</html>